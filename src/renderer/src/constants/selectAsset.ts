export interface SelectAssetType {
  title: string
  placeholder: string
  noResults: string
}

export const selectAssetData: SelectAssetType = {
  title: 'Select an asset',
  placeholder: 'Search',
  noResults: 'No results found',
}
